-Pembeli Buku
-Produk Buku

Backand 
-node js
-express
-Body Parser
-cors
-mysq

DATABASE
========

Cara membuat backend
-buat folder
-buka di visual studio code -> click kanan -> buka folder menggunkan vsc
-buka terminal(gitbash)
-npm -yv -> hasilnya package json
-npm i express - body - parser mysql cosr

TABEL DATABASE
==============

1. User/Pembeli
	nama tabel : user
	Query : 
		- id INT NOT NULL AUTO INCREMENT Primary Key
		- id INT NOT NULL AUTO INCREMENT Primary Key
		- username VARCHAR (255) NOT NULL
		- email VARCHAR(255) NOT NULL
		- password VARCHAR(255) NOT NULL
		- nama VARCHAR(255)
		- role VARCHAR(255)
		- created_at TIMESTAMP - updated_at TIMESTAMP

2.Produk buku (nama tabel, produk)
-id -> integer
nomor_produk -> string
halaman -> string
judu_buku -> string
gendre -> string
tahun_terbit -> string
penulis -> string
harga -> int
isbn ->
diskon ->
created ->
update ->

